routes:  
  /portfolio/:
    controller: channel
    filter: tag:Favourite-Posts
    template: portfolio-list

collections:
  /blog/:
    permalink: /blog/{slug}/
    template: post-list

  /:
    permalink: /{slug}/
    template: home

taxonomies:
  tag: /tag/{slug}/