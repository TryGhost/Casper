{{{html}}}
<aside class="gh-post-upgrade-cta">
    <div class="gh-post-upgrade-cta-content" style="background-color: {{accentColor}}">
        {{#has visibility="paid"}}
            <h2>This post is for paying subscribers only</h2>
        {{/has}}
        {{#has visibility="members"}}
            <h2>This post is for subscribers only</h2>
        {{/has}}
        {{#if @member}}
            <a class="gh-btn" data-portal="intro4" style="color:{{accentColor}}">Try 4 weeks premium</a>
        {{else}}
            <a class="gh-btn" data-portal="premium" style="color:{{accentColor}}">Try 4 weeks premium</a>
            <p><small>Already have an account? <a data-portal="signin">Sign in</a></small></p>
        {{/if}}
    </div>
</aside>
